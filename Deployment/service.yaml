apiVersion: v1
kind: Service
metadata:
  name: lms-app-service
  namespace: teamtwo
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
  selector:
    app: lms-app
